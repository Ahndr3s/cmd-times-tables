# Notas
First Node app
```
    Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Base de la tabla de multiplicar            [número] [requerido]
  -l, --list     Muestra la lista de multiplicaciones impresas en el archivo
                 creado                              [booleano] [defecto: false]
  -h, --limit    Numero limte del 2do factor                            [número]
```