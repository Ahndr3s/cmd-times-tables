{
  "name": "03-bases-node",
  "version": "0.0.1",
  "description": "Multiplication table generator that creates a file with the base that the user define",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "base6": "node app --base=6"
  },
  "author": "Anhdres",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.0.0",
    "yargs": "^17.5.1"
  }
}
